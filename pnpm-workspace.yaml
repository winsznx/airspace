packages:
  - "packages/*"
  - "apps/*"
  - "workers/*"

# esbuild and workerd ship prebuilt binaries and need their install scripts.
allowBuilds:
  esbuild: true
  workerd: true

onlyBuiltDependencies:
  - esbuild
  - workerd
